| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |---
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |name: chalkboarding
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |description: >
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |  Turns an idea into a figure that looks like a teacher drew it on a chalkboard
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |  and animates it like a lesson: things appear in the order the teacher would
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |  draw them, an eraser replays. One HTML file. Use when the user says
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |  "chalkboard figure", "chalk figure", or "blackboard diagram", wants a sketch
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |  or idea turned into one, or iterates on any *_chalk.html file.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |---
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |# Chalkboard figures
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |One self-contained HTML file that looks like a hand-drawn chalkboard and plays
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |like a lecture. Embeds via iframe, prints as the final frame, exports to MP4.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |## Core Principles
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |1. **Divide and conquer.** Never conceive and render in the same step. Every hard visual goal splits into a structure step and a manifestation step. When something feels too hard to produce directly, find the split.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |2. **Ask before drawing.** One native questionnaire before any mock, a second one to approve the mock. Even when the user handed you a description.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |3. **One idea per figure.** A teacher's board, not a dashboard. More than ~2 panels or ~3 beats per panel means two figures.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |4. **Be fun, and hand-drawn.** Great teachers reach for silly, concrete, everyday examples, and draw them in chalk. Stick figures and chalk pictograms, not emoji. Abstract boxes-and-arrows is the fallback, not the default.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |5. **The board shows, the caption tells.** No verdicts, morals, sources, or explanatory prose on the slate. Deliver a one-line caption separately.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |6. **Idempotent `paint(t)`.** One function renders any moment from scratch. Replay, reduced motion, and print all fall out for free.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |7. **Trace, don't freehand.** You have weak spatial intuition. Get shapes from references, then render them in chalk.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |The three splits behind principle 1:
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Split | Structure | Manifestation |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || --- | --- | --- |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || ASCII mock | what the figure says | how it looks |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Tracing | the shape | the drawing of it |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || `paint(t)` | the story's beats | the rendering of any moment |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |## Design Rules (NON-NEGOTIABLE)
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |All wired into `template.html`. Full spec in `design-system.md`.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Rule | Value |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || --- | --- |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Frame | wood `#7A5230` |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Slate | deep green `#12291d` with faint radial chalk-dust lighting |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Chalk ink | `#F5F4EF`, secondary `rgba(245,244,239,.55)`, nothing dimmer |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Font | `PencilPete.ttf` for everything on the slate, relative `url("PencilPete.ttf")`, copied next to the output |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Hand-drawn waver | SVG turbulence filters `#slip1..3` applied probabilistically by the end-of-body script; longer element = more likely to waver |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Micro-imperfection | alternating rotations ±0.25° to 1.4°, irregular radii like `10px 8px 11px 7px` |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Animation | one `paint(t)`, beat constants at the top, single rAF loop, eraser replay button, freeze on final frame |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Fixed heights | status, tally, badge lines use `height` + `line-height` + `nowrap`, never `min-height`; board height never changes mid-play |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || File name | `<topic>_chalk.html`, append `-v2`, `-v3` when iterating |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |Gotchas:
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- Every new visual class goes into the slip script's selector list.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- SVG `<text>` is not covered by the script. Put `filter:url(#slip1)` on the SVG element directly.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- Never accumulate state inside the rAF tick. Build DOM once, reveal in `paint`.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |## Anti-Slop Rules
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |The style works because it looks like a human drew it. Anything mass-produced breaks that.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Rule | Do | Don't |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || --- | --- | --- |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Emoji budget | at most 1-2 per figure, each a named character that carries data (the 🐕 that grows with its share) | rows or grids of repeated emoji, emoji as bullets, emoji as decoration |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || People and objects | chalk stick figures and pictograms as inline SVG, round-capped strokes, 5-10 paths each (see the walker and runner in `examples/example4_go_to_school.html`) | 🧑‍🎓 x 40, 👤 icons, clip art, photos |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Crowds and quantities | draw the scale: a row of chalk desks, tally marks, a bar that shrinks, a pie cut thinner | repeating a glyph N times |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Mock vs manifest | emoji are fine as placeholders in the ASCII mock | carrying mock emoji into the HTML |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Labels | short, concrete, sometimes lowercase, like a teacher's shorthand | title-case headings, marketing lines, exclamation marks |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Symmetry | slight rotations, uneven radii, a line that wavers | pixel-perfect alignment, identical repeated elements |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |If a mock uses an emoji, decide at manifest time: is this the one character (keep it) or a crowd (draw it in chalk)?
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |## Board Content Rules
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || On the slate | Off the slate (caption) |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || --- | --- |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || names, numbers, one-line status | verdict or moral ("the price is ...") |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || the character and its data | source or attribution ("after X, 'title'") |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || done badges, beat labels | prose that explains the figure |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |One wink per figure (a venue in-joke, a deadpan verdict line). Seasoning, not the meal.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |---
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |## Phase 0: Intake Questionnaire
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |Run ONE questionnaire with all three questions in a single call. Use the native structured UI (AskUserQuestion in Claude Code); otherwise one message with lettered options. Pre-fill the recommended option from anything the user gave and mark it "(Recommended)". Do not propose a mock or graphics before the answers are in.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || # | Header | Question | Options |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || --- | --- | --- | --- |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || 1 | Idea | What is the one thing this figure says? | 2-3 one-line read-backs of their text, each a different emphasis (mechanism / payoff / contrast) |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || 2 | Layout | What does it look like? | A. Line chart: a crossing or gap is the point. B. Two-panel contrast: same input, two methods. C. Step-by-step walkthrough: one beat at a time. D. Architecture diagram: components and flows |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || 3 | Motion | How does it move? | Plays itself once (default) / buttons the reader toggles / slider the reader drags |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |Write each layout option in terms of THEIR figure, not generic text. "Other" is where they describe it in their own words.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |If a figure in `examples/` shares the shape they chose, you may open it in Phase 2 as a starting point. Do not go looking for the closest one, and never bend their picture to fit ours.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |## Phase 1: ASCII Mock
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |Layout mistakes are 10x cheaper to fix in ASCII than in styled HTML. Never skip this for anything non-trivial.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |Before drawing boxes, ask: what's the everyday story here, and who's the character? Rejection sampling is "The best pet is a ___" with a cat and a dog. Parallel denoising is painting a kangaroo. Path selection is racing to school. Emoji are allowed here as placeholders; in Phase 2 they become chalk drawings (see Anti-Slop Rules).
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |The mock shows, at roughly the real aspect ratio (~940px wide):
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- every panel, box, and row with its border and label
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- placeholder text where real labels go
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- the replay button, bottom-right
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- a **beat table** underneath: what appears at which second
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |```
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |+--------------------------------------------------------------+
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || time ->  |0s----1s----2s----3s----4s----5s---...---10s|      |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || +----------------------------------------------------------+ |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || : VANILLA DECODING                          (done · 10.0s) : |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || : [Happiness][can][be][found]...   <- one chip per second  : |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || : ============------------------   <- chalk progress fill  : |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || :  target pass 4 of 10...          <- status line          : |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || +----------------------------------------------------------+ |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || +----------------------------------------------------------+ |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || : SPECULATIVE DECODING                    (done · 3.3s ✓)  : |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || : ([..5 chips..]) ([..5 chips..])  <- chunks per round     : |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || +----------------------------------------------------------+ |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe ||                                              [eraser/replay] |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |+--------------------------------------------------------------+
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |Beats: t=0 start · t=1.65 chunk 1 · t=3.3 chunk 2 + done badge
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |       t=10 vanilla done · t=11.5 freeze (loop end)
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |```
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Symbol | Meaning |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || --- | --- |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || `:` | dashed panel border |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || `[x]` | chip |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || `(...)` | grouped chunk |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || `=` / `-` | filled / unfilled progress |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || `<-` | annotation on anything that moves |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |**Approve with a SECOND questionnaire.** One question, header "Mock":
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- Approve as drawn (Recommended)
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- One or two concrete layout alternatives you can see (flat grids vs 3D slab, stacked vs side by side)
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- Change beats or labels (they say what via Other)
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |Iterate until approved. The mock is the contract: panel count, reading order, and beat table carry 1:1 into Phase 2. If no user is available (batch run, unattended agent), write the mock and beat table to `<topic>_mock.md` next to the output and proceed as if approved.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |## Phase 2: Build the HTML
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |Read in this order:
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || File | For |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || --- | --- |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || `template.html` | the working skeleton to start from |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || `design-system.md` | exact colors, board construction, font, waver system |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || `animation-patterns.md` | recipes: chips, progress fills, playheads, SVG draw-on, pixel grids, sliders |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || `worked-examples.md` | prompts, mocks, and tricks behind the richest examples |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |Open the example closest to the approved mock as a starting point.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |### Complex imagery: trace, don't freehand
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |Freehanding a kangaroo or an opera house from imagined coordinates produces mush. Split into **trace** (shape from a reference) and **render** (fill in chalk).
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Subject | Method |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || --- | --- |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Unclear concept | find 1-2 reference images first, study them, then draw |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Pixel grid | `python3 scripts/trace_bitmap.py <image> --size 32` turns any PNG (Twemoji emoji, logo, silhouette) into the `X`/`.` bitmap the grid recipe consumes |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || SVG pictogram | take a handful of landmark points from a reference (head circle, spine, limb angles), connect with round-capped strokes. Trace the skeleton, not the outline |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe || Trivial geometry | freehand is fine: stick figures, arrows, boxes, simple charts |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |## Phase 3: QA Before Delivering
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |```
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |bash scripts/qa.sh <file> --beats <start>,<mid>,<end> --replay
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |```
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |Pick the three beats from your own beat table: just after t=0, right after the key beat, past your `END` constant. The defaults `0.3,3,12.5` fit a 10-12s figure, not yours. First run installs Playwright into `~/.cache/chalkboarding` by itself. A sandboxed agent may need the sandbox off for that one command.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |Look at every image and check:
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- [ ] nothing overflows the slate; chips wrap instead of clipping
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- [ ] board height identical at t=0 and at END (measure both screenshots)
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- [ ] final frame is self-sufficient: print and reduced-motion readers get every label and the done state
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- [ ] contrast: primary chalk, secondary at .55 alpha, nothing dimmer
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- [ ] waver visible on long strokes, labels still legible
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- [ ] the `--replay` shot matches the start shot
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- [ ] emoji count is 0-2 and none repeat; every crowd is drawn in chalk
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |Deliver the file plus the one-line caption.
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |## Phase 4: Share & Export
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |After delivering the file and caption, ask with the native questionnaire (header "Export"):
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- MP4 (Recommended): crisp, for X, slides, and GitHub attachments (click to play)
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- GIF: softer and larger, but autoplays and loops in a README `<img>`
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- Both
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |- No thanks
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |If they decline, stop. Otherwise run the wrapper. It installs Playwright by itself on first run and needs ffmpeg (`brew install ffmpeg`).
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |```bash
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |bash scripts/export.sh <file>                 # <file>.mp4 next to the input
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |bash scripts/export.sh <file> --gif           # also <file>.gif
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |bash scripts/export.sh <file> --seconds 9     # match the figure's END constant
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |bash scripts/export.sh toggle_chalk.html --click "#mRelax@4"   # click a control mid-clip
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |```
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |
| Embedding | `background:transparent` on body, `html{zoom:0.8}`, `postHeight()` posting `{chalkHeight, chalkSrc}` verbatim. The `body.standalone` rule centers the board when the file is opened directly instead of in an iframe |Then tell the user the output path and size, and that redoing the export overwrites the same file.
